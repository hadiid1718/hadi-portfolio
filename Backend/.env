# Server
PORT=8080

# MongoDB
MONGO_URI=mongodb+srv://hadeedmalik86:hadeed123@mern.srhcac0.mongodb.net/consultation?retryWrites=true&w=majority&appName=USLEEP

# Auth
JWT_SECRET=replace_with_a_long_random_string

# Predefined admin account (created automatically on first server start)
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=change_me
ADMIN_NAME=Hadeed Ul Hassan
ADMIN_ROLE=superadmin

# Chatbot (Google Gemini)
# Get a key at https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key
GEMINI_MODEL=gemini-2.5-flash

# Outgoing email (used for the admin "Reply" flow — sends the reply directly to the user)
# Gmail: enable 2FA on the account, then create an "App Password" and use it as EMAIL_PASS.
EMAIL_SERVICE=gmail
EMAIL_USER=hadeed.hassan990@gmail.com
EMAIL_PASS=rmutptbrhraaflac
EMAIL_FROM_NAME=Hadeed Ul Hassan

# Optional: use a custom SMTP host instead of EMAIL_SERVICE
# EMAIL_HOST=smtp.yourprovider.com
# EMAIL_PORT=587